name: location_hub
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.9.2
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_background_service: ^5.0.0
  flutter_local_notifications: ^17.0.0
  geolocator: ^11.0.0
  signalr_netcore: ^1.3.6 # Specifically for connecting to /hubs/ URLs
  permission_handler: ^11.3.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true